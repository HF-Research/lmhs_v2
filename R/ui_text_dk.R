ui_main_title <- "Results"
ui_about_tite <- "Methods"

choose_respondent_type <- enc2utf8("Choose respondent type")
respondent_type_choices <- enc2utf8(c("Patient", "Pårørende"))

choose_topic <- enc2utf8("Vælg emne")
topic_choices <- enc2utf8(c("A", "Pø"))

choose_question <- enc2utf8("Vælg spørgsmål")
question_choices
